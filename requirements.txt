# Extracellular GPX4 immune suppression - reproducible runtime environment
# Generated 2026-08-12 based on a managed Python 3.13 virtual environment (versions tested in practice).
# Install: python -m venv .venv && .venv/Scripts/pip install -r requirements.txt
# Notes: scanpy/anndata versions pinned explicitly (in response to the reproducibility audit "scanpy version unspecified";
#        anndata 0.13.x is incompatible with pandas 2.2.x (StringDtype na_value=... argument missing),
#        anndata 0.12.0 + pandas 2.2.3 + zarr 3.x verified compatible.

numpy==2.1.3
pandas==2.2.3
scipy==1.14.1
lifelines==0.30.3          # standard log-rank test (replacing hand-written implementation)
scanpy==1.12.3             # single-cell analysis (h1 / cross-cancer single-cell)
anndata==0.12.0            # compatible with pandas 2.2.x + zarr 3.x (0.13.x requires pandas 3.x)
matplotlib==3.9.2
seaborn==0.13.2
requests==2.32.3           # cBioPortal / HPA API
