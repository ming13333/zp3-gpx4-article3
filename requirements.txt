# 细胞外 GPX4 免疫抑制 — 可复现运行环境
# 生成于 2026-08-12，基于 managed Python 3.13 虚拟环境实测可用版本。
# 安装：python -m venv .venv && .venv/Scripts/pip install -r requirements.txt
# 说明：scanpy/anndata 版本已显式声明（回应实证审查「scanpy 版本未声明」；
#       实测 anndata 0.13.x 与 pandas 2.2.x 不兼容——StringDtype(na_value=...) 参数缺失，
#       anndata 0.12.0 与 pandas 2.2.3 + zarr 3.x 兼容，已实测验证）。

numpy==2.1.3
pandas==2.2.3
scipy==1.14.1
lifelines==0.30.3          # 标准 log-rank 检验（替代手写实现）
scanpy==1.12.3             # 单细胞分析（h1 / 跨癌种单细胞）
anndata==0.12.0            # 与 pandas 2.2.x + zarr 3.x 兼容（0.13.x 需 pandas 3.x）
matplotlib==3.9.2
seaborn==0.13.2
requests==2.32.3           # cBioPortal / HPA API
